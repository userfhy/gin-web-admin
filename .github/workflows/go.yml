name: Go
on:
  push:
    branches:
    - dev
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
      - uses: actions/checkout@v6
        with:
          go-version: '>=1.26.0'
      - name: Build
        run: go build -v .
